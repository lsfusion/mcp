"""lsFusion RAG fill — bootstrap and ingest tooling for platform/docs (type-first: docs/<type>/en).

See RAG-PLAN.md at aggregate root for the full design. This package is
NOT shipped in the runtime mcp Docker image — `.dockerignore` excludes
`fill/` so the service container stays minimal.
"""
