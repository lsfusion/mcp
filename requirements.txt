mcp[cli]>=1.6.0,<2.0.0
fastmcp>=2.0.0,<3.0.0
openai>=1.50.0  # vector_stores.search (retrieve_docs_tool)

# fill.* runtime deps. Token counts and header-split heuristics are
# inputs to section_payload_hash AND to _merge_short_siblings' floor/cap
# decision (chunker.py SHORT_SECTION_FLOOR_TOKENS=50, MERGE_SOFT_CAP_TOKENS=256).
# A patch-version bump in tiktoken or langchain-text-splitters can shift
# token counts at those boundaries and silently re-shape merged sections
# without a CHUNKER_VERSION bump. Use EXACT pins to keep byte-level
# output stable across environments; rotate intentionally and bump
# CHUNKER_VERSION when these pins change.
tiktoken==0.12.0
langchain-text-splitters==1.1.2
python-frontmatter==1.1.0